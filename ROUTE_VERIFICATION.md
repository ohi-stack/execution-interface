# Route Verification
The route verification suite validates HTTP 200 for required public routes, validates /api/health payload status=ok, validates /api/manifest route inventory parity, verifies HTML responses for page routes, and verifies /process requires x-omos-key.
