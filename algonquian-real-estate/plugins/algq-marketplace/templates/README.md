# Templates

The current production release renders Deal Marketplace views through PHP renderer classes in `includes/`.

This directory is retained for WordPress package structure compatibility and future overrideable template partials. No runtime template partials are required for version 1.0.1.
