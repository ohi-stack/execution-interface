# Known Limitations

1. OHI pipeline currently uses simulated candidate model outputs.
2. Prompt deployment guide is implementation-ready but does not include provider-specific SDK wrappers.
3. Protocol scoring is deterministic and static in this version (no adaptive tuning).
4. No UI for the public entry endpoint; currently API-first delivery.
