# Next-Step Backlog (Deferred)

## Priority 1
- Add OpenAPI paths for `/api/omos/classify` and `/api/omos/identity-definition` in `openapi/openapi.yaml`.
- Add schema validation middleware for OMOS endpoints.

## Priority 2
- Connect OHI pipeline module to real model providers (non-placeholder).
- Persist pipeline audit events to storage with retention controls.

## Priority 3
- Provide thin web page wrapper for public identity definition endpoint.
- Add CI workflow to enforce protocol schema compatibility on pull requests.
