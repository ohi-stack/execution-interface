# Runtime Manifest
`/api/manifest` returns service identity, version, environment, canonical host, route inventories, WordPress bridge metadata, compatible hosts, app bridge, and commerce bridge URL.
