# omos-core

Main OMOS runtime scaffold.

## Modules
- `kernel/` Onegodian decision algorithm runtime.
- `alignment/` prompt and policy enforcement hooks.
- `identity/` identity classification logic.
- `api/` API contracts and endpoint handlers.
- `sdk/` client SDK stubs for JS/Python.
