export default function OfferingDetail({ id }: { id: string }) {
  return <section className="space-y-3 rounded-lg border bg-white p-5"><h2 className="text-xl font-semibold">Offering {id}</h2><p className="text-slate-700">No performance claims are shown. Review disclosures and legal notices before activating workflows.</p></section>;
}
