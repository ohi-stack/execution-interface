{
  "name": "wallet-core-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Wallet Core Engine (WCE) v0.1 modular TypeScript monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "test": "npm run -ws test",
    "typecheck": "npm run -ws typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
