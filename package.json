{
  "name": "issuer-qrv-portal",
  "version": "1.0.0",
  "description": "Issuer Portal MVP for QRVID issuance, verification, and revocation on the qrv.network system.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "next build",
    "start": "NODE_ENV=production node server.js",
    "db:init": "node scripts/init-db.js",
    "check": "node --check server.js && node --check controllers/authController.js && node --check controllers/issuerController.js && node --check controllers/recordsController.js && node --check controllers/verifyController.js && node --check routes/auth.js && node --check routes/issuer.js && node --check routes/records.js && node --check routes/verify.js && node --check services/registryService.js && node --check utils/qrvid.js && node --check utils/api.js"
  },
  "keywords": [
    "qrv",
    "issuer",
    "nextjs",
    "express",
    "postgres",
    "supabase"
  ],
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "next": "^15.3.0",
    "pg": "^8.13.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
