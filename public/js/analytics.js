(function(){const track=(event,payload={})=>console.log('[analytics]',JSON.stringify({event,payload,at:new Date().toISOString()}));document.querySelectorAll('.btn').forEach((el)=>el.addEventListener('click',()=>track('cta_click',{href:el.getAttribute('href')})));if(location.pathname.startsWith('/tools/'))track('tool_launch',{path:location.pathname});if(location.pathname==='/manifest')track('manifest_request');})();
