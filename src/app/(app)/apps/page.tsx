import { appStructureLayers, coreRoutes } from '@/lib/onegodian-content';
export default function AppsPage(){return <main className="space-y-6"><h1 className="text-3xl font-bold">OneGodian App Structure Standard</h1><p>Every OneGodian app, plugin, module, and bridge must follow the OneGodian App Structure Standard so each system can operate as a public-facing page, logged-in dashboard, and admin/control panel.</p><section><h2 className="font-semibold">Required Layers</h2><ul className="list-disc pl-5">{appStructureLayers.map((l)=><li key={l}>{l}</li>)}</ul></section><section><h2 className="font-semibold">Required Core Routes</h2><ul className="list-disc pl-5">{coreRoutes.map((r)=><li key={r}>{r}</li>)}</ul></section><p className="text-cyan-300">Every OneGodian app must be usable in three ways: public-facing page, logged-in dashboard, and admin/control panel.</p></main>}
