import { NextResponse } from 'next/server'; import { ODC_TOKEN, apiSuccess, cacheHeaders } from '@/lib/odc';
export function GET(){return NextResponse.json(apiSuccess({address:ODC_TOKEN.contractAddress,chainId:1,network:ODC_TOKEN.network,standard:ODC_TOKEN.standard,decimals:18,maximumSupply:ODC_TOKEN.maximumSupply,sourceVerificationStatus:'verification-pending',contractOwner:null,ownershipStatus:'verification-pending',mintingCapability:'verification-pending',pausingCapability:'verification-pending',blacklistingCapability:'verification-pending',upgradeability:'verification-pending',proxyStatus:'verification-pending',administrativeRoles:null,auditStatus:'verification-pending',dataProvider:process.env.EXPLORER_API_URL?'configured':'not-configured',lastChecked:new Date().toISOString()}),{headers:cacheHeaders})}
