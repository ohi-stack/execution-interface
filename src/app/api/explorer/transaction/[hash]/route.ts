import { NextResponse } from 'next/server'; import { transactionHashSchema, ProviderUnavailableError, getExplorerProvider } from '@/lib/explorer'; import { apiError, apiSuccess } from '@/lib/odc';
export async function GET(_:Request,{params}:{params:{hash:string}}){const result=transactionHashSchema.safeParse(params.hash);if(!result.success)return NextResponse.json(apiError('INVALID_TRANSACTION_HASH',result.error.issues[0].message),{status:400});try{return NextResponse.json(apiSuccess(await getExplorerProvider().getTransaction(result.data)))}catch(e){if(e instanceof ProviderUnavailableError)return NextResponse.json(apiError('PROVIDER_UNAVAILABLE',e.message),{status:503});throw e}}
