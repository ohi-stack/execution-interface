import type { Metadata } from 'next'; import { PublicInfoPage } from '@/components/PublicInfoPage';
export const metadata: Metadata={title:'Developers',description:'Developer guidance for canonical, read-only ODC integrations.',alternates:{canonical:'/developers'}};
export default function Page(){return <PublicInfoPage eyebrow="Developers" title="Build from the canonical record." intro="Public token and manifest endpoints support read-only integration discovery. Transactional platform capabilities are not currently offered through this portal." status="Live" cards={[{title:'Canonical endpoints',copy:'Use /api/token and /api/manifest for versioned public identity and route information.'},{title:'Ethereum verification',copy:'Integrations must validate Ethereum Mainnet, chain ID 1, and the complete canonical contract address.'},{title:'Safe integration',copy:'Never request seed phrases or private keys. Treat planned services as unavailable until their status changes to Live.'}]}/>}
