'use client';
import Link from 'next/link'; import { useState } from 'react'; import { PageHero } from '@/components/ODC';
const docs=[['getting-started','Getting started','What ODC is, contract verification, network, wallets, receiving, sending, and fees.'],['token','Token documentation','Identity, maximum supply, decimals, compatibility, name history, and utility status.'],['contract','Contract documentation','Address, source verification, permissions, controls, audit distinction, and integration.'],['wallets','Wallet & security','Network selection, approvals, seed protection, phishing prevention, and finality.'],['developers','Developer documentation','APIs, rate limits, manifest, errors, versioning, and changelog.'],['disclosures','Legal & disclosures','Risk, utility, tax, liquidity, advice, third-party, and fraud notices.']];
export default function Docs(){const [q,setQ]=useState('');const shown=docs.filter(x=>x.join(' ').toLowerCase().includes(q.toLowerCase()));return <><PageHero title="Documentation center" intro="Searchable guidance for using, verifying, and integrating ODC safely."/><div className="docs-search"><label htmlFor="docs-q">Search documentation</label><input id="docs-q" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search topics"/></div><div className="grid two">{shown.map(([slug,title,desc])=><Link className="card" href={`/docs/${slug}`} key={slug}><p className="eyebrow">Guide</p><h2>{title}</h2><p>{desc}</p><strong>Read guide →</strong></Link>)}</div><aside className="warning">ODC materials are informational and do not constitute financial, legal, investment, tax, securities, or accounting advice.</aside></>}
