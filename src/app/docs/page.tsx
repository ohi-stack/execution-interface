import Link from 'next/link'; import { PageHero, CTA } from '@/components/ODC';
const docs=[['architecture','Architecture','System boundaries and read-only data flow.'],['api','API','Versioned endpoint contracts and response envelopes.'],['deployment','Deployment','Build, deploy, verify, and rollback.'],['security','Security','Controls, secret rotation, and incident handling.'],['wordpress-bridge','WordPress Bridge','Safe integration with the OneGodian ODC Plugin.'],['disclosures','Disclosures','Risk and scope statements.'],['version-history','Version History','Platform release record.'],['production-checklist','Production Checklist','Release validation evidence.']];
export default function Docs(){return <><PageHero title="Documentation" intro="Production guidance for operating and integrating with the ODC platform."/><div className="grid two">{docs.map(([slug,title,desc])=><Link className="card" href={`/docs/${slug}`} key={slug}><h2>{title}</h2><p>{desc}</p><span className="eyebrow">Read guide →</span></Link>)}</div><CTA/></>}
