import Link from 'next/link';
export function Hero({ eyebrow, title, subtitle, primaryHref = '/framework', secondaryHref = '/algorithm' }: { eyebrow: string; title: string; subtitle: string; primaryHref?: string; secondaryHref?: string }) {
  return <section className="relative overflow-hidden rounded-[2.5rem] border border-[#D8B35A]/20 bg-white/[0.055] p-6 shadow-2xl shadow-black/30 backdrop-blur md:p-10"><div className="absolute -right-24 -top-24 h-72 w-72 rounded-full bg-[#6F3CFF]/30 blur-3xl" /><p className="text-xs font-black uppercase tracking-[0.28em] text-[#F0D98A]">{eyebrow}</p><h1 className="mt-4 max-w-5xl text-[clamp(2.6rem,8vw,6rem)] font-black leading-[0.92] tracking-[-0.07em] text-white">{title}</h1><p className="mt-6 max-w-4xl text-lg font-semibold leading-8 text-slate-200 md:text-2xl">{subtitle}</p><div className="mt-8 flex flex-wrap gap-3"><Link className="rounded-full bg-[#D8B35A] px-6 py-4 font-black text-black shadow-gold" href={primaryHref}>Explore Framework</Link><Link className="rounded-full border border-white/15 bg-white/[0.06] px-6 py-4 font-black text-white" href={secondaryHref}>View Algorithm</Link></div></section>;
}
