'use client';
import { useRouter } from 'next/navigation'; import { useState } from 'react';
export function ExplorerSearch(){const [value,setValue]=useState('');const [error,setError]=useState('');const router=useRouter();function submit(e:React.FormEvent){e.preventDefault();const q=value.trim();if(/^0x[a-fA-F0-9]{40}$/.test(q))router.push(`/explorer/address/${q}`);else if(/^0x[a-fA-F0-9]{64}$/.test(q))router.push(`/explorer/transaction/${q}`);else setError('Enter a valid Ethereum address or transaction hash.')}return <form className="explorer-search" onSubmit={submit} noValidate><label htmlFor="explorer-query">Search Ethereum activity</label><div><input id="explorer-query" value={value} onChange={e=>{setValue(e.target.value);setError('')}} placeholder="Address or transaction hash" autoComplete="off"/><button className="button" type="submit">Search</button></div>{error&&<p role="alert">{error}</p>}</form>}
